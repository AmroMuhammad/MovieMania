//
//  AppColors.swift
//  MovieMania
//
//  Created by Amr Muhammad on 11/04/2026.
//

import SwiftUI

enum AppColors {
    static let primary = Color.red
    static let secondary = Color.gray
    static let background = Color(uiColor: .systemBackground)
    static let secondaryBackground = Color(uiColor: .secondarySystemBackground)
    static let label = Color(uiColor: .label)
    static let secondaryLabel = Color(uiColor: .secondaryLabel)
    static let chipSelected = Color.red
    static let chipUnselected = Color(uiColor: .secondarySystemBackground)
    static let chipTextSelected = Color.white
    static let chipTextUnselected = Color(uiColor: .label)
}
